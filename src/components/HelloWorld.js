// Import the necessary modules from React and ReactDOM
import React from "react";

import { createRoot } from "react-dom";


// Define a functional component named HelloWorld
function HelloWorld () {
    // Return the JSX that will be rendered as HTML
        return (

            <div>

                <h1>Hello World!</h1>

            </div>

        );

    }
// Export the HelloWorld component as the default export of this module
export default HelloWorld;
